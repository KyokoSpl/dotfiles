local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "rust", "javascript", "typescript", "vue", "html", "css" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
